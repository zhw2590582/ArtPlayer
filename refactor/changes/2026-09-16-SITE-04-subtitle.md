# SITE-04 字幕指南与语义映射

起点：`de45f5b978a69aaae3b466325ac4dce35bd0dee2`。本批属于既定实施文档，
SITE-04 保持 doing；不进入用户保留的三轮正式复盘。

双语高级属性的字幕章节补齐配置默认值、switch 浅合并与配置归属、转换回调、
Promise/原生加载阶段、轨道与 cue 引用、渲染事件和 escape、资源归属和取消。
说明空 URL 不清除轨道、原生 track 错误不会追溯拒绝已经兑现的 Promise、低层
init/createTrack/destroyEvent 的边界，以及根历史声明与 runtime 准确声明的区别。
核心运行时、声明、入口和旧36个 Run Code 片段均未修改。

新增27条配置、管理器、cue/track及实例入口映射，累计358/963，605条仍待核对。
之前分组源码哈希保持不变；高级属性指南除字幕章节外的内容，经过 LF 归一化后与
起点逐字一致，才更新相应整页哈希。没有将字幕章节的核对扩展到其他播放/事件成员。

Node24.21.0 / Yarn1.22.22：34项测试通过，0失败/跳过，1449.102ms。
VitePress9.85秒，Yarn整体11.47秒；LLM生成/只读检查、站点清点和demo检查通过。
两页共六份 strict/NodeNext TS 代码通过（含原有四份）。六组浏览器页面、108次
代码转发一致。Chromium153.0.8010.12、Firefox155.0、WebKit26.6各通过22项
字幕断言：真实原生track加载SRT/VTT、seek后的cue/渲染、事件、样式/转义、错误、
替换及销毁资源回收。中文页面截图目视检查通过。

迟到请求使用受控fetch包装，故意忽略AbortSignal，以验证取消后的结果隔离；其他
字幕通过HTTP路由响应、真实Blob和原生track加载。没有据此声称完成播放/解码、
iOS/macOS原生全屏或真机验收。21库候选build证据仍有效，其他发布门槛未改。
详情、文件哈希和日志见[证据](../baselines/site04-subtitle.json)。

docs/document与LLM文件由项目脚本生成。回退本提交恢复指南和生成文件即可，
无需回退核心行为。继续其余核心声明与文档核对；不推送或发布。
