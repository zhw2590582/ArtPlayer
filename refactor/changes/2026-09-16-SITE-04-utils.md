# SITE-04 公共工具函数指南

起点：`bd3f3c0689abda6b3f5a40ea30e68ff82e001e78`。继续实施期文档核对，
不进入正式复盘。SITE-04 保持 doing。

双语静态属性指南按环境/类型、DOM/样式、事件/测量、字幕/图片/文件、数据/错误/
调度补齐全部50个工具成员。明确原生返回值、失败条件、字符串HTML插入、类型
历史差异，以及公开loadImg缩放结果和vttToBlob的调用者URL归属。说明mergeDeep
并非深拷贝、数组展开规则、debounce/throttle返回和receiver、catch能力判断等边界。
没有修改核心实现、公开声明或旧24个Run Code片段。

新增105条声明映射：根/runtime各50个工具成员、3个结构输入成员、2个静态入口。
累计463/963，500条仍待核对。既有分组源码哈希不变，静态属性指南在utils章节
之外与起点逐字一致（LF归一化后），才刷新相应整页哈希；不把这些条目算作105个
不同的浏览器用例。

Node24.21.0 / Yarn1.22.22：57项既有测试通过，0失败/跳过，1355.0013ms。
VitePress9.40秒，Yarn整体10.89秒；LLM生成/只读检查、站点清点及demo检查通过。
两份strict/NodeNext TS示例通过。六组页面、72次代码转发一致；Chromium
153.0.8010.12、Firefox155.0、WebKit26.6各41项工具断言通过，包含真实DOM、
CSS、图片缩放、Blob读取与调用者回收。英文页面截图目视检查通过。

不据此证明跨域图片权限、下载完成、真机safe-area或完整播放验收。21库build
证据仍有效，其他发布门槛未变，无推送/发布。证据与文件指纹见
[报告](../baselines/site04-utils.json)。docs/document及LLM由脚本生成；回退本提交
只恢复文档/生成内容，不改变核心行为。继续构造配置、事件及播放等剩余文档。
