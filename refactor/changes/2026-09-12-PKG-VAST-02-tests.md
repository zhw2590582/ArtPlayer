# PKG-VAST-02 行为、失败与生命周期测试

起点4f39d6f2。本步建立回归基线，未修改VAST生产源码、声明、依赖或dist。
新增`yarn test:vast`并接入`test:unit`；没有新依赖或锁文件变更。

45项Node测试覆盖当前入口、实际发布1.0.0源码及冻结工作区1.2.0源码：29项正常
及特有行为、16项历史缺陷观察。精确替换SDK导入，保留原wrapper逻辑；晚到回调
通过保存旧SDK订阅函数复现，并非要求SDK在destroy后正常派发事件。

实际核心5.1.7已从npm下载并冻结SHA512/SHA256、manifest及197成员，新增持续验证。
浏览器server增加5.1.7映射，继续保存各核心实际执行字节的manifest。90项浏览器测试
覆盖5.1.7/5.4.1/候选核心、三个wrapper实现及Chromium/Firefox/WebKit；包含异步
注册、受控SDK拒绝后的真实主片播放、DOM显隐、显式清理/重建、主片切源以及历史
销毁后晚到初始化。实际Google IMA广告流程仍未验证，不因本次通过关闭SDK-07。

第一轮90项通过，随后补充主片切源后重建广告的断言并重新执行。新增Node文件的
格式问题已修正；一次eslint自动修复把新归档测试的node:test错误替换为vitest，
直接运行发现缺失模块后恢复Node runner并加精确lint说明，没有安装vitest。
最终执行结果和输入哈希见 [验证记录](../baselines/vast-behavior-validation.json)。

VAST-LIFE-01提升为已复现并保持open，具体10类问题及修复方向见
[测试维护](../vast-validation.md)。这里只观察/固定问题，03逐项修复并增加候选验收
断言；不把缺陷观察通过等同于生产修复。初始化和类型兼容冲突仍由03/04处理。

包内模块地图随03实际拆分更新，本步提供测试文件/命令/边界说明，避免描述尚不存在
的生产架构。版本升到2.0.0由REL-09统一处理；VPN例外尚未使用。

回退本提交只移除本步测试、浏览器core映射、脚本接入和文档台账，不改变生产行为。
任务完成独立本地提交PKG-VAST-02；没有push、tag、publish或merge。

最终：90项浏览器测试全通过，无跳过/重试；完整本地CI885项通过（817单元、14工程、
54基线），严格类型和37份核心声明无漂移。没有将受控广告SDK验收扩大为实际供应商服务。
