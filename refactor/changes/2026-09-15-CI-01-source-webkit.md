# CI-01 完整 Windows WebKit 源码回归

固定 HEAD `ae56937d3beb981e78b97f83ef557cdc62135900`、Node24.21.0、
Yarn1.22.22，运行 `yarn test:browser:source --project=webkit --workers=2`。
没有文件/grep过滤、重试、诊断SDK或媒体前缀覆盖；全程跟踪文件未改，结束时工作区干净。

## 实际结果

174文件、1664项：1515通过、11失败、138跳过、0重试，1816.373秒。
实际浏览器Windows WebKit26.6，工具进程和Yarn均退出1。没有修改测试来隐藏结果。
原JSON、截图、trace及日志归档于 `.cache/ci-source-webkit-full-report`，
[机器证据](../baselines/ci-source-webkit-validation.json)保存每文件统计、跳过原因、
失败断言和附件、归档路径及SHA-256。

| 失败 | 实际阶段与证据 | 处置 |
| --- | --- | --- |
| Audio8项，新旧核心/新旧插件/video与audio受限响应 | 全部在audio-buffering.spec.js:30未观察到trusted waiting且time>0.3；初始双时钟>0.3检查均已通过 | AUDIO-BUFFER-01仍open；不能由初始时钟推断成功解码、真实饥饿或恢复 |
| JASSUB3项，实际npm1.1.0与三种核心 | 首次字幕像素一直为0；worker/WASM/字体HTTP200，worker ready，注册1次帧回调但交付0次，视频约6.7秒、质量计数全零 | 与已冻结的JASSUB-NATIVE-FRAME-01旧版缺陷一致；原候选修复和其他测试证据保持各自范围 |

Audio请求实际只发送96KiB视频或32KiB音频。七项没有waiting记录；一项仅有
trusted=true/time=0/readyState=2，未满足原条件。失败均发生在显式gate.release()
之前，未完成双媒体恢复断言。与早期独立原生前缀探针的起播结果分别记录：
不能将本轮初始时钟通过写成所有控制都能起播，也不能把早期无推进结论套到本轮。
仍需对应原生后端或其他实际支持平台的缓冲证据，不添加模拟waiting来满足测试。

JASSUB原生源码测试默认仍选择发布1.1.0；本轮没有在这三个失败用例中运行候选插件。
状态显示只收到初始空render，未发送字幕帧需求。已有候选帧时钟修复见
[PKG-JASSUB-07](2026-09-14-PKG-JASSUB-07-vendor.md)，安装模式的旧/新对照见
[CI安装记录](2026-09-15-CI-01-jassub-installed.md)。旧包再次失败不撤销候选修复，
也不证明本次运行额外完成了该候选原生场景。测试仍返回失败，没有改成预期失败。

## 能力与通过数

138项跳过全部来自明确的既有能力条件：ASR WebAudio58、DASH MSE48、HLS MSE32。
每文件原因及数量保留在机器证据，包含ASR首次失败后无法继续WebAudio恢复的情况。
这不是138项功能通过，Windows WebKit也不是Safari真机。

1515项通过中包括旧缺陷观察、受控窗口/SDK和不支持分支；不能解释为1515项原生
解码能力。上一轮修复的Monaco全声明用例在本次全量中通过。章节、字幕等此前的
间歇问题和Auto Thumbnail首帧门槛不因本轮其他用例通过而关闭。

## 交付与后续

CI维护说明和环境矩阵增加实际全量入口，区分不同HEAD/集合与历史初始快照。
CI-01继续doing，216 done、21 doing、43 todo不变；没有新增完成任务或放宽依赖。
公开代码、声明、生成资源、版本、依赖、锁文件和工作流均未改，无需重建分发。
计划/风险生成检查、风险负对照及严格工具链检查通过。完整安装矩阵、远端CI和
物理设备仍待验证，三轮发布复盘仍保留。没有推送、部署或发布。

后续优先核对source范围仍只默认选择历史包的用例，补足明确的候选输入证据，
同时保留历史控制和原失败；不要用source入口名称代替实际输入来源审查。
回退本检查点只撤销本次记录和索引，不改变生产或测试运行行为。
