# BASE-02：冻结公共 API 与默认值

日期：2026-09-10；分支 codex/compatible-modernization；起点 833cc116。

## 交付

扩充 BASE-HARNESS-01 的真实发布包夹具，保存核心 5.4.0 / chapter 1.1.0 的公共 API 描述符和默认值。加入静态配置、继承属性查找、绑定 query、构造选项身份、插件同步返回和销毁返回断言。服务器给报告附加其提供的发布成员和夹具来源指纹。

内置浏览器最终两次 reload 的 14 项检查全部通过，快照完全相同，error/warn 日志为空；UA 为 Windows / Chrome 152.0.0.0，语言 zh-CN。报告见 [public-api.json](../baselines/public-api.json)，细分的契约、固定测试 ID、重跑命令、限制和后续责任见 [api-coverage.md](../baselines/api-coverage.md)。

新增 api.mjs 检查持久报告或比较新采集报告，明确区分“校验已保存的报告”和“重新执行浏览器”。新增两项 Node 测试验证报告必须完整且来源匹配，以及描述符/方法/默认值/继承接口回归会被拒绝。不提供自动覆盖冻结快照的命令。

## 兼容与验证

没有修改生产源码、公开声明、依赖或发布产物。固定包依旧由归档完整性与成员哈希校验后提供。语言默认值按真实环境关系验证后规范化；其他差异不自动忽略。API 比较记录可观察形状，反射中的内部字段变化仍需要兼容评估，不是把原内部架构永久锁死。

- 14 个浏览器断言、两次最终快照一致性通过。
- 新增 2 个报告/比较器测试及原归档/HTTP 基线测试通过，已由 test:baseline 纳入 CI。
- 夹具/比较器语法、计划/链接、Git 差异与完整 ci:check 验证通过。
- 不宣称真实播放、完整生命周期、全部插件、类型或候选新旧组合通过；对应任务继续待办。

## 接续与回退

每项契约缺口与下一任务记录在 api-coverage.md。下一步 BASE-03 验证事件和生命周期；Chrome 不可用时继续使用内置浏览器。工作区/发布基线差异、第三方 SDK、真实设备和发布门槛保持。

本任务独立本地提交，主题包含 BASE-02。可撤销该提交恢复旧夹具；冻结数据保留于 Git 历史，不能直接更新快照隐藏后续回归。没有推送或发布。
