# SITE-04 静态配置与全局常量

起点：`9afbf9940dd019191aa6680b00812bb1c7d8e8b1`。继续实施期的文档语义核对，
SITE-04保持doing，不进入用户保留的正式复盘。

四份双语静态/全局属性指南补齐34个全局字段的默认值和读取时机，区分构造时捕获、
事件时读取、排队延迟和已经创建的UI。修正config不是默认选项的说明、resize防抖、
恢复提示阈值、滚动示例应监听view，以及关闭媒体重置仍执行其他销毁清理。
两种语言各改两个滚动回调和一个销毁示例注释，其余92个总片段中的内容保持不变。
没有改核心源码、公开声明、字段值或分发入口。

新增44条声明映射：34个全局字段、两套Config的8个数组字段和2个静态入口。
累计507/963，456条仍待核对。之前各组源码哈希不变；静态属性指南除config段外
与起点逐字一致（LF归一化后），才刷新之前模板/工具组的整页哈希。

Node24.21.0 / Yarn1.22.22下117项相关测试通过，0失败/跳过，1640.9033ms。
VitePress9.66秒，Yarn整体11.16秒；LLM生成/只读检查、inventory与demo检查通过。
四份strict/NodeNext类型片段通过。四页在三浏览器形成12组页面检查，只对config
和两个修正滚动示例执行18次转发；全部92个片段均完成语法和允许差异比较。

Chromium153.0.8010.12、Firefox155.0、WebKit26.6各52项断言通过，其中34项
是默认值比对，不冒充独立行为场景。行为检查包含共享事件清单的构造快照、后续
实例读取新值、旧设置项不重建、STYLE不重新注入、CONTEXTMENU的动态读取、
window:scroll/view事件、网页全屏位置恢复、媒体重置开关和其他生命周期清理。
中文页面截图目视检查通过。真机触摸/旋转/全屏、SDK重试及播放不由本批验收。

证据、输入哈希及日志见[报告](../baselines/site04-globals.json)。21库build仍有效，
其他发布门槛未改，无推送/发布。产物由项目脚本生成；回退本提交仅恢复文档和
对应生成文件。继续其余构造选项、事件及播放接口的语义核对。
