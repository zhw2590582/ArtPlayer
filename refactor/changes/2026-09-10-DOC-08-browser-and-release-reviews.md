# DOC-08 浏览器验证与多轮发布复盘

- 日期：2026-09-10。
- 基线提交：`1fa0a578`；分支：`codex/compatible-modernization`。
- 提交主题：`docs(refactor): [DOC-08] require browser evidence and release reviews`。
- 用户要求：记录 Chrome 验证方案，重构完成后通过多次全局复盘达到 npm 发布条件。

## 交付

新增 release-reviews.md，记录 Chrome 交互与自动化/真机的分工、三轮检查、问题分类、逐项修复提交、复验及候选内容绑定。同步根指令、文档入口、测试、发布、路线和 AI 接续要求。

新增 REVIEW-01/02/03 待办；REL-05 必须依赖 REVIEW-03。保留原真机、回退和正式发布门槛。共 202 任务，其中 8 个规划任务完成、194 项待办，生产重构仍未开始。连接 Chrome 的事实只证明工具发现了该环境；未执行播放器测试。

## 验证与限制

- 计划生成、依赖无环、22 包覆盖、文档链接与生成同步检查。
- 专项依赖检查：REL-05 和 REL-06 包含三轮复盘、REL-03 真机和 REL-04 回退前置；既有 198 项任务的 ID、范围、状态保留。
- git diff --check 和暂存范围检查，提交后核实 Git 状态和日志。
- 本任务仅修改维护文档及任务数据，没有运行生产/浏览器测试、安装依赖、推送或发布 npm。

## 后续与回退

下一项仍是 BASE-01，复盘在对应候选和验证前置完成后执行。可独立回退本 DOC-08 提交，不影响生产代码；不得将文档完成计作复盘通过。
