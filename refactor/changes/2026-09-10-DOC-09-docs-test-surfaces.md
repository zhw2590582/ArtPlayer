# DOC-09 记录已有 docs 测试入口

- 日期：2026-09-10；分支：`codex/compatible-modernization`。
- 基线提交：`3af9becf`。
- 提交主题：`docs(refactor): [DOC-09] map existing browser test pages`。
- 用户要求：将已有 HTML 和在线编辑器用于真实测试的方案记入维护文档。

## 变更

新增 docs-browser-testing.md，记录六类页面、Monaco/示例参数、核心 prod 开关与插件独立加载、移动重定向、文档 smoke 的 100ms 完成方式及覆盖限制。规定状态隔离、真实播放断言、候选内容映射与逐项任务归属。

补充 BASE-04、ENG-05、SITE-01/02、EX-03 的交付/验收文字，保留原依赖和范围；没有重复创建实施任务。只新增已完成的文档任务 DOC-09，原 202 项任务状态不变。现有 203 项任务：9 项规划完成、194 项待办，生产重构未开始。

## 验证

已静态核对 HTML、common.js、dev.js、build-test.js 和 iframe/chapter 示例。运行计划生成/同步、依赖无环、包覆盖、文档链接和 Git 差异检查；核对原任务 ID/范围/状态/依赖未变。未运行浏览器、生产测试或构建，未修改页面/生成产物，未推送或发布。

## 后续与回退

下一项仍为 BASE-01；具体页面基线和自动化按上述实施任务执行。独立回退本提交只影响计划文档；不能把本次记录当作真实测试通过。
