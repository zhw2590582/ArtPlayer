# SITE-04 文档内容核对完成

起点：`54e5cbf49caf7bf1a500d003d89822ca7fbbc4f5`。本提交完成计划内的文档内容
任务，不是用户保留的 REVIEW-01/02/03，也不代表全项目可发布。

补齐最后63条核心声明：显示状态、原生/网页全屏、PiP、迷你模式、AirPlay、尺寸
与坐标、比例/翻转/封面、截图、缩略图和字幕偏移；剩余reset声明链接已有生命周期
结论。核心累计963/963，逐条ID与当前inventory完全一致，无重复/遗漏；全部分组
源码/指南哈希与生成HTML锚点已校验。以前已核对段落不变，只更新指南整体哈希。

纠正两个语言的state示例：state赋值只关闭其他模式，不开启指定模式。现在用
mini=true进入，再用state='standard'退出。94个示例中仅这两份改变，其余92份
逐字不变；全部通过语法检查，10份TS代码块通过strict/NodeNext。六组页面与36次
代码转发通过，中文显示说明截图目视通过。没有改变生产运行时、声明和分发接口。

Node24.21.0 / Yarn1.22.22：122项相关回归通过，0失败/跳过，1584.4914ms。
VitePress10.79秒、Yarn12.51秒；LLM、inventory、demo检查通过。

现有source浏览器套件选取显示、尺寸、截图与缩略图契约：93项通过，0失败/跳过/
重试，78.28秒，每种浏览器31项。包含旧发布版与当前源码构建的实际全屏归属、
DOM恢复、PNG像素、下载文件名、Blob释放责任、跨域SecurityError与雪碧图格子。
尺寸用例中显式覆盖媒体宽高，是受控布局检查，不称为全部真实解码。历史版本
的无效尺寸、样式恢复、缩略图格子等缺陷有分别的旧/新断言，没有放宽候选要求。

Chromium153.0.8010.12与Firefox155.0记录到原生PiP窗口和继续播放，WebKit26.6
记录标准PiP不可用；该分支通过只证明正确识别不支持，不是WebKit PiP或Safari/iOS
验收。AirPlay接收设备、手机、SDK、长时播放及安装包矩阵保持各自门槛。

任务收尾另外验证20个生态包的40份双语指南：与各包既有语义核对记录的源文件
哈希一致，从对应记录提交到当前HEAD，源码、public/types/runtime、README、
ARCHITECTURE及package.json没有变化。所有SITE-04前置任务均已done。核心963条
映射、生态逐包结论及文档站已有迁移记录共同满足内容核对范围；完整证据见
[报告](../baselines/site04-display.json)和[包级跟踪](../site-content-review.md)。

SITE-04由doing改为done，任务统计225 done /21 doing /39 todo。21库候选build
证据仍有效，但发布台账仍有其他阻断；没有推送、部署或发布。SITE-05/EX-03负责
接续全量页面和demo执行，SITE-06等负责其余文档/消费者工作，正式复盘等待用户。
回退本提交恢复本批指南、映射、生成文件与SITE-04状态。
