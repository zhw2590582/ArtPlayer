# DOC-07 重构计划复审与执行顺序优化

- 日期：2026-09-10。
- 分支：`codex/compatible-modernization`。
- 基线提交：`d7b348f3`。
- 提交主题：`docs(refactor): [DOC-07] improve sequencing and release gates`。
- 范围：任务依赖、实施与发布门槛、测试策略及维护文档；未改生产源码。

## 发现与处理

| 问题 | 原计划证据 | 处理 |
| --- | --- | --- |
| 试点太晚 | PKG-CHAPTER-04 有 32 个递归前置任务，包含 CORE-14 | chapter 03/04 改为依赖现有核心与工具；新增 PILOT-01，在 CORE-01 之前完成浏览器/类型/tarball 闭环 |
| 启动基线过大 | BASE-01 要求各包 npm 历史基线，ENG-01 依赖它 | BASE-01 先冻结核心和试点，其余发布证据在各包 01 步补齐 |
| 外部设备间接阻塞文档和候选准备 | SITE-04 和 REL-01/02 祖先包含 PKG-CAST/VAST/DPIP/MASK-05 | 文档交叉核对依赖源码/类型步骤；候选准备依赖核心与构建；真实验收保留在包集成和发布门槛 |
| 环境与版本矩阵尚无独立任务 | 文档提出范围要求，但任务中没有完整字段和负责步骤 | 新增 BASE-08，区分格式、浏览器能力、真实设备、未知项和证据归属 |
| 初始 CI 可能因历史问题无法推进 | 已观察第三方类型冲突；全部包初始严格检查没有失败分类方案 | 新增 ENG-10，建立有复现证据的失败台账、逐模块严格门槛和偶发测试处理 |
| 缺少明确无障碍回归步骤 | 原核心测试强调热键，没有独立的可访问名称、焦点和键盘导航任务 | 新增 CORE-23，并加入最终核心验收依赖；不预断言页面存在已复现缺陷 |
| 发布台账与回退演练太晚 | REL-01 等待 SITE/EX/MOD 全部完成，REL-04 等待全部真机验证 | 新增 REL-08 提前登记逐包准入；版本方案与回退演练提前，正式发布仍受实际范围门槛限制 |
| 工具链步骤存在无关依赖 | MOD-02 的脚本整理必须等待 Bun 试点 | 移除该非必要依赖，最终 MOD-05 同时收敛工具与 Bun 结论 |

## 不变的要求

全部 22 包仍在范围内。保留每包旧支持范围核心和最终核心的集成验证、真实环境证据以及每任务独立提交。没有把被阻塞的测试改成通过，也没有新增推送或发布授权。

任务 ID 均保留，新增任务而不重排旧 ID 含义。已有完成任务的事实不重写。具体更新后的依赖与状态以 tasks.json 为准；父阶段编号不代表执行先后。

## 验证

- 运行 plan.mjs 的生成、同步、依赖无环、22 包覆盖和本地链接检查。
- 专项检查：PILOT-01 的祖先中没有 CORE；CORE-01 必须依赖试点；SITE-04、REL-01/02 不再间接依赖 Cast 等包的设备验收任务；REL-03 仍汇集最终外部验证。
- 所有既有任务 ID 保留，新增任务 ID 唯一，只有 DOC-07 新增完成状态；没有实施任务被标完成。
- 执行 git diff --check、暂存内容检查及提交后的 Git 状态/日志核对。

本轮没有运行生产或真实浏览器测试，因为生产代码未改。计划校验不代表播放器兼容性已通过。

## 后续和回退

下一项仍为 BASE-01，其工作量现在限定为可启动核心和试点的最小发布基线。按任务逐项实施、验证和提交。必要时独立回退 DOC-07 计划提交，不影响生产功能。
