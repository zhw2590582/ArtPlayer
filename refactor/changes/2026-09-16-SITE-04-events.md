# SITE-04 事件与同步派发契约

起点：`0a060e6088ded006235cfdbf5064b8ff014d842a`。本批继续实施期文档核对，
SITE-04 保持 doing，不进入用户保留的三轮正式复盘。

补齐双语事件指南：Emitter 的上下文、同步快照、重复监听、once 重入、off、异常
和销毁后的用户订阅；原生媒体/文档/窗口转发清单；自定义事件参数与时机；根入口
历史声明与 runtime 准确类型的区别。纠正 video:complete 的旧说明，它不是默认
转发事件。没有修改核心源码、公开类型、入口或事件行为。原有 90 个 Run Code
片段逐字保持不变；新增两份严格 TypeScript 样例。

新增 120 条声明语义映射，累计 627/963，336 条仍待核对。此前所有分组的源码和
指南哈希均保持一致。本批覆盖 Emitter、根实例事件重载、静态入口、Events、
SubtitleUpdateEvents 和 runtime BuiltinEvents，不把声明条数当成独立行为数量。

Node 24.21.0 / Yarn 1.22.22 下，131 项相关测试通过，0 失败/跳过，1522.8842ms。
VitePress 构建 9.40 秒，Yarn 整体 11.03 秒；LLM 生成/检查、inventory 和 demo
检查通过。两份 strict/NodeNext 样例通过；90 个既有片段语法和内容比对通过。
双语页面在三浏览器形成 6 组页面检查、24 次代码转发，每页只转发前四个示例。

Chromium 153.0.8010.12、Firefox 155.0、WebKit 26.6 各 30 项断言通过，包括
同步派发、异常传播、原生与方法事件区别、真实本地 MP4 播放/暂停/seek 参数、
UI 参数和销毁状态。中文截图目视检查通过。键盘/指针使用合成 DOM 事件；这些
检查不代表真机、SDK、全部示例、长时播放、codec 矩阵或发布验收通过。

第一次 Chromium seek 断言失败保留在报告中。8082 是 9 月 13 日启动的旧服务，
没有 Content-Length/Accept-Ranges，Range 请求返回全部文件和 200。完全不加载
Artplayer 的原生 video 同样报告 seekable [0,0]，把请求的 1 秒钳回 0；等待十秒
仍未形成有效范围。使用当前 startDevServer 在隔离端口 8083 提供同一文件，
Range 返回 206/16 字节，原生范围恢复 [0,90.046009]，seek 到 1 秒成功；原来的
严格播放器断言随之在三浏览器通过。没有修改播放器或服务源码、放宽断言或
覆盖失败证据；现有 8082 进程保留。后续媒体测试须核对实际服务响应，不能仅
根据磁盘上的新代码假定运行进程已更新。

输入哈希、完整通过结果和失败对照见[报告](../baselines/site04-events.json)。
21 个库的候选构建证据仍有效，其他发布门槛未关闭。生成文件来自项目脚本；
回退本提交恢复本批文档及其生成内容，无运行时回退需求。未推送或发布。
