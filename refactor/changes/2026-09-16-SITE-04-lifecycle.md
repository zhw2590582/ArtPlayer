# SITE-04 实例生命周期与媒体能力

起点：`cec5049c4016b5f0f30fe19c8d8bde0d0e2f2704`。SITE-04 保持 doing，
本批为实施期文档核对，不进入用户保留的正式复盘。

双语实例指南补齐实例登记、身份与六个状态字段、SDK 集成位置、内部组件服务、
reset/destroy 的同步返回、重入和异常清理，以及原生/代理媒体的可选能力。
静态指南纠正 env/build：冻结的 npm 5.4.0 与当前 6.0.0 均没有这两个运行时字段，
根声明保留历史形状，runtime 入口不声明它们。没有为错误文档新增运行时字段。

新增 61 条声明映射，累计 829/963，剩余 134 条。以前分组的源码哈希不变；
class 指南仅 env/build 段落变化，property 的 CSS 段落不变，均完成内容比对后
更新指南哈希。118 个既有 Run Code 片段逐字不变并通过语法检查，10 份 TS
代码块通过 strict/NodeNext 检查。没有改变生产运行时、声明或公共接口。

Node 24.21.0 / Yarn 1.22.22 下，146 项相关测试通过，0 失败/跳过，
2081.9708ms。VitePress 构建 10.66 秒，Yarn 整体 12.48 秒；LLM 生成/检查、
inventory、demo 检查通过。21 库候选 build 证据仍有效，其余发布门槛保留。

Chromium 153.0.8010.12、Firefox 155.0、WebKit 26.6 各 38 项生命周期断言通过；
覆盖实际焦点转移、原生 MP4 就绪与 reset、资源归属、重复/重入销毁、用户监听器
抛错后的清理、保留 DOM 与容器复用。12 组双语页面、42 次选定示例转发通过，
中文实例指南截图已目视检查。转发不等于执行全部 118 个示例；可选媒体 API
只记录能力字段类型，没有发起 PiP/AirPlay 等请求，不能代替真实设备验收。

首轮 WebKit 英文 property 页面等待 networkidle 超时，原报告保留。独立同 URL
导航成功，同时观察到外部 reCAPTCHA 请求超时；这不足以断定首轮失败原因。
只对 WebKit 重跑同一套检查，源文件、生成页面、候选哈希与超时设置不变，
完整通过后与原 Chromium/Firefox 结果合并，明确保留失败、诊断和重跑报告。
未修改运行时、增加等待期限或删除断言来制造通过。

证据见 [报告](../baselines/site04-lifecycle.json)。下一步继续剩余播放与显示接口。
未推送或发布；回退本提交只恢复文档、映射和生成内容。
