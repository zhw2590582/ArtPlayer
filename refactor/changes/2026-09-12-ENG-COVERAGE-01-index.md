# ENG-COVERAGE-01 契约覆盖与报告对应

起点6abac891。创建22包×12契约归属、29个版本依据（22工作区/7发布），从真实断言
逐条登记10个稳定ID。其他用例待索引，历史JSON按任务列出，不把旧总数当作当前契约
通过。维护方式见 [契约覆盖维护](../contract-coverage.md)。

check:contracts/test:contracts接入ci:check，五文件44项产生10个精确观察，保留候选
字节指纹、实际事件、当时定义及来源；输入/定义变化标旧，skip/todo/失败不算通过。
没有生产源码或运行时依赖修改。

七组测试覆盖路径/版本/任务/ID/报告反例，并启动真实Node验证失败、skip/todo。首轮
发现Windows绝对路径不能直接作ESM reporter specifier，独立复现后改file URL。生产
runner原本用相对specifier，不受影响。失败日志保留，完整结果在完成前归档。

文档入口明确早期未运行描述是历史目录，后续查progress与包记录，没有批量改成通过。
未运行远端Actions或发布。回退本步移除CI两个命令及相应脚本/策略/报告/文档，原有
包行为和测试不受影响。逐成员映射、浏览器/类型解析和设备/SDK验收由包任务及REVIEW/
REL继续；本任务不批准发布。

最终七组反例通过，主测试链918项及44项重复观察执行通过，专项lint通过。补齐空清单
拒绝和不同ID重复同一断言的拒绝，避免隐式发现或虚增索引。两个阶段实际报告分别
归档，旧候选保持历史；完整记录见 [验证证据](../baselines/contracts-validation.json)。
