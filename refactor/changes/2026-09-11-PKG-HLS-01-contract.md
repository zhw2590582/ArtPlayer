# PKG-HLS-01：固定 HLS control 发布与兼容边界

已核对源文件、两个 SVG、声明、README、官方示例以及 npm 1.1.0 六文件归档。
见 [契约](../baselines/hls-control-contract.md) 和 [指纹](../baselines/hls-control-release.json)。
单独新增基线，不覆盖 BASE-01 的历史数据。三份元数据相同，两段主逻辑归一化一致，
registry gitHead 的版本不匹配如实登记，不当作源码来源证明。

新增只读重验入口 `node refactor/scripts/hls-contract.mjs`，复用归档完整性工具，逐文件验证，
从固定 Git 提交读取历史源码；不要求迁移后的现行源码保持旧实现。没有新增依赖或生产脚本。
离线缓存可重跑，缓存缺失时仅从固定 registry URL 下载并验 hash。脚本不执行发布代码。

结构/类型/兼容：本步冻结工厂、同步 update、媒体绑定、菜单名/形状、getName 调用、选择顺序、
入口和未声明的支持窗口。空轨道残留、旧回调、Auto、声明遗漏和示例清理分别落到 HLS-02～06。
未修改生产源码或声明；类型及浏览器行为验证属于后续任务，不以本步静态核对代替。

验证：固定归档 SHA-512/SHA-256、六成员 hash、历史源码 hash、三份元数据及两个函数对照通过；
计划检查、基线测试与 Git 差异检查通过。没有运行全量核心播放矩阵，因为本步不改播放器。
回退本提交只撤回 HLS 契约与重验工具，不影响生产产物。下一步 HLS-02，完成后另行提交。
