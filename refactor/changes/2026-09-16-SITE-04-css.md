# SITE-04 CSS 变量与主题接口

起点：`24011ea26678ab7b52f9d10b3fd589f388a6a0cc`。继续实施期内容核对，
SITE-04 保持 doing，不进入用户保留的正式复盘。

双语实例属性指南新增 43 个 CSS 变量的默认值、使用位置、模式覆盖与布局归属，
以及 cssVar/theme 的真实读写行为。根类型的数字返回/9999 字面量、数字零触发
读取、字符串零执行写入、行内覆盖移除和配置对象不回写都明确区分。保留已有
API、声明、样式和运行时，不因文档核对改变用户视觉效果。

逐文件核对确认 42 个声明变量有 CSS 消费者；--art-mini-progress-height 仅保留
声明和 2px 默认值，核心没有读取它。--art-controls-height 是布局观察器写入的
内部测量值，不把它冒充已有的声明输入。移动端和全屏类的变化记录为样式规则，
不当作设备能力承诺。

新增 48 条映射（43 个变量、根/runtime cssVar/theme 的五条声明记录），累计
768/963，195 条仍待核对。以前分组的源码和指南哈希全部保持一致。原有 94 个
Run Code 片段逐字不变，新增两份严格 TS 样例。

Node 24.21.0 / Yarn 1.22.22 下，最终 47 项相关测试通过，0 失败/跳过，
1594.0244ms。最终 VitePress 9.06 秒，Yarn 整体 10.65 秒；LLM 生成/检查、
inventory 和 demo 检查通过。两份 strict/NodeNext 样例及 94 个既有片段的语法/
内容比对通过。

Chromium 153.0.8010.12、Firefox 155.0、WebKit 26.6 各 83 项断言通过，其中
43 项是基础值、8 项是模拟移动端类的默认值比对，不称为 83 个独立交互场景。
其余验证字符串/零值、实例隔离、主题消费、样式层叠、控制栏测量、网页全屏
及闲置变量无效果。六组双语页面检查与 12 次 cssVar/theme 示例转发通过；
中文说明和变量表截图已目视检查。无原生全屏、真机或全量示例验收结论。

验证中保留并纠正三项过程问题：LLM 产物未同步导致文档测试失败，生成后重跑；
新 TS 样例漏处理 $player 可空性，改为空值安全访问后重建；浏览器断言把压缩
CSS 的 .75 写死成 0.75，对照证明未操作实例仍保持 .75，改为比较写入前后
实际值。没有修改生产类型、放宽零值/隔离行为断言或覆盖失败日志。

证据与输入哈希见[报告](../baselines/site04-css.json)。当前隔离 8083 服务用于
浏览器验证，旧 8082 未改动。21 库候选 build 仍有效，其他发布门槛保留。
未推送或发布；回退本提交仅恢复文档、映射及生成内容。继续剩余播放/实例接口。
