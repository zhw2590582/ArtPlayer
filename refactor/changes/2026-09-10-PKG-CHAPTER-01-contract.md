# PKG-CHAPTER-01：冻结章节插件迁移边界

核对源码、Less、声明、README、示例和固定 npm 1.1.0 内容，形成
[chapter 契约](../baselines/chapter-contract.md)。发布与工作区的声明/README/manifest 一致，
插件函数及样式注入语句语法归一化 hash 相同；完整归档完整性复用 BASE-01。

明确工厂无参和 update 无参的区别、同步返回值、时间规则、调用方数组原地变更、DOM/CSS、
setBar、一次 metadata 订阅、样式注入与清理边界。保留现有分发入口、全局名与旧核心集成；
支持窗口缺少历史最低版本声明，没有自创范围。BASE-TYPE-02 在下一步扩展可选参数声明。

记录空白标题残留、非有限时间、切源更新责任及销毁后 update，作为包内测试/改进项目。
没有修改生产代码或把静态审查当成这些行为已通过浏览器验证。

验证：归档完整性、三个元数据文件对照、语句提取/归一化对照、计划/文档链接和 Git 差异检查。
回退本提交只移除契约记录。下一步 PKG-CHAPTER-02 补包特有真实浏览器断言和可选参数类型修复。
