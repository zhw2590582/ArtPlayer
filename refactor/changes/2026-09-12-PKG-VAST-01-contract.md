# PKG-VAST-01 真实契约冻结

起点29692d28。核对实际发布VAST1.0.0、未发布工作区1.2.0、SDK1.21.0/1.21.2及
发布关联核心5.1.7。详情见 [契约表](../baselines/vast-contract.md) 和
[归档](../baselines/vast-release.json)。本任务不修改生产源码、类型、依赖或产物。

新增可持续运行的归档验证和四项契约测试；实际发布JS/legacy导出直接执行，两套
源码在仅替换SDK的受控边界运行。证实发布callback拥有id/$container及已创建Player，
工作区移除别名并改为懒创建；两者实际均异步而声明写成同步。另确认SDK公开类型
引用dev-only依赖，当前最小消费者在两种TS版本各复现两个TS2307。

API-06/07/09/11差异、默认设置及初始化时机、资源清理和外部SDK验证分别登记为
VAST-CONTEXT-01/TYPE-01/LIFE-01/DIST-01和既有SDK-07，指派02～06处理，不以
本次基线通过宣称这些问题已修复。Ads类型批准不扩大到VAST异步声明差异。

用户已更正VPN跳过授权对象为VAST；若脚本持续被规则阻止，可记录并跳过其实际
网络验证，由用户后续修复。正常的本地契约/生命周期/类型改造继续执行。

所有检查和输入指纹见 [验证记录](../baselines/vast-contract-validation.json)。
本步53项基线测试全通过（其中新增VAST四项），脚本及仓库lint、计划与147项风险
台账检查通过。没有生产变更，本步未重复完整CI、构建或真实广告媒体验收。
首次VM测试遗漏CustomEvent，SDK fallback访问document导致失败；提供平台原生
CustomEvent后实际导出检查通过，不修改发布bundle。生产SSR仍由分发任务单独测试。

回退本任务仅撤销本步文档/台账/测试，不改第三方归档或生产文件。完成后独立本地
提交PKG-VAST-01，无推送、tag、发布。下一步02建立SDK失败、回调、事件和销毁测试。
