# DOC-03：校正 AI 接续入口

核对当前源码后，发现根 `AGENTS.md` 仍列出 MediaBunny 五个不存在的旧 `.js` 文件，
通用包示例仍是 `src/index.js`，代码风格要求也未区分已迁移的 TypeScript。
这些是维护入口的过期信息，不是重新开展用户保留的正式复盘。

本次改为实际 `.ts` 路径，优先链接 MediaBunny 的模块维护地图；通用包布局补充内部
TS 模块和架构说明，并明确旧根声明与可选 `/runtime` 的区别，引用已批准的类型规则。
同时记录普通浏览器与长测共用 8084，运行中必须保持被测输入不变，不能用旧报告代替
活跃进程或终结结果。

验证仅针对文档：核验 AGENTS 中非占位的仓库路径存在，计划/链接校验与 diff 检查通过。
没有改源码、测试、产物、依赖或公开 API；不重复运行播放器测试，也不干扰正在进行的
MediaBunny 长测。DOC-03 已完成状态不变，本次是其维护记录的独立修正提交。
