# DOC-11 全包大版本升级策略

- 日期：2026-09-10；分支：`codex/compatible-modernization`。
- 基线提交：`a9197f59`。
- 提交主题：`docs(refactor): [DOC-11] require next major versions for all packages`。
- 用户要求：所有包分别升级到自身下一大版本，如 5.4.1 → 6.0.0，插件同理。

## 交付

核对全部 22 个 workspace manifest，新增 version-policy.md 目标清单，替代旧的“不自动提升全部 major”表述。根指令、发布/CI/CD/复盘和决策同步；REL-01/02、CI-03、REVIEW-03 补充版本验收要求。

仅记录策略，未改 package.json、锁文件、历史基线或生成产物。各包独立版本与旧 API 兼容要求保留，不把主包版本套到全部插件。文档包也列入目标，npm 发布资格仍逐包核实；私有根工作区不作为 npm 发布包。

## 验证与后续

运行目标表与 22 个 manifest 的对应/major+1 断言、计划生成/依赖/覆盖/链接检查及 Git 差异检查。原 208 项任务 ID、范围、状态和依赖保持，新增 DOC-11 已完成；共 209 项任务，11 项规划完成，198 项待办。

没有运行生产或浏览器测试，没有推送、打 tag 或发布。下一项仍为 BASE-01；实际改版本由 REL-01/02 组织独立任务提交，候选测试之前生效。可独立回退本 DOC-11 文档提交，不影响生产代码。
