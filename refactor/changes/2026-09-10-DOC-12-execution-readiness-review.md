# DOC-12 开发前的计划复审

- 日期：2026-09-10；分支：`codex/compatible-modernization`；基线：`9b9739ab`。
- 提交主题：`docs(refactor): [DOC-12] close execution and release planning gaps`。
- 范围：计划依赖、验收定义及计划校验器；没有生产代码改动。

## 发现与修正

| 问题 | 证据 | 修正 |
| --- | --- | --- |
| 首轮复盘重新被专项环境阻塞 | REVIEW-01 -> MOD-05 -> MOD-04 -> PKG-MASK-06/05 | 首轮仅依赖 MOD-01/02/03，第二轮继续等待完整 MOD-05 和设备验收 |
| 全包版本落实只在文字中要求未来拆任务 | REL-02 仅依赖版本方案，没有显式版本修改步骤 | 新增 REL-09，方案后落实 manifest/锁/依赖/日志，候选构建必须依赖它 |
| BASE 使用浏览器证据，但正式服务在后续 ENG | ENG-05 依赖 BASE-04，ENG-08 依赖 BASE-06 | 明确基线使用旧包与最小可重跑探针，正式 ENG 接入，不引入反向依赖 |
| 同一产物标准不适用于所有 workspace | vitepress manifest 只有站点脚本，没有核心式 main/module/types 入口 | BASE-05/REL-08 记录实际发布类别及替代验收；全部版本目标保持 |
| 契约和测试缺少强制对应关系 | 契约表、任务表和测试规范分别存在，但没有明确索引交付 | BASE 开始登记，ENG-09 收敛契约/版本/测试 ID/报告/责任映射 |
| SSR 容易被过度理解 | 核心 constructor 非浏览器明确抛错 | 分开 import、静态模板、浏览器挂载、非浏览器构造错误，不新增服务端播放器 API |
| 校验只保证无环，不能发现语义依赖回退 | 之前首轮设备依赖通过了校验 | plan.mjs 增加试点、首轮/次轮设备、版本/CI/复盘发布门槛和实施任务可达性检查 |

## 验证

计划生成/同步、22 包覆盖、本地链接、语义依赖及 Git 差异检查通过。校验器以故意加入首轮设备依赖、删除版本准备前置、移除 CI 门槛、加入依赖环的负例验证应失败；负例结束恢复原任务数据。node --check 通过。

原 209 项任务 ID、范围和状态保持，新增 DOC-12 完成、REL-09 待办。现有 211 项任务：12 项规划完成、199 项待办。没有运行生产/浏览器/远端 CI，没有改变包版本或发布。

## 下一步和回退

建议开始 BASE-01，不再扩写假想实现细节。模块路径、工具版本、性能方案与复现缺陷随开发补充；保持任务依赖和证据同步。可整体回退本提交，同时恢复任务依赖与校验器，避免留下不匹配规则。
