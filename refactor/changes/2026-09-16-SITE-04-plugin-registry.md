# SITE-04 插件注册指南与语义映射

起点：`5bf1a36183550a0e48a31b6d2317a3c6c4ed578d`。本批仍属实施文档，
不进入用户保留的 REVIEW-01/02/03；SITE-04 保持 doing。

双语编写插件指南补齐工厂参数/this、构造时 registry 尚未赋值、同步返回、同 realm
Promise、ready 独立、名称与计数、属性描述符、错误归属、next 和销毁后迟到结果。
明确插件自行清理资源，返回对象的 destroy 不是自动钩子；增加准确 runtime 类型与
模块扩展示例。旧六个 Run Code 片段保持不变，核心源码、声明和分发接口没有修改。

核心清单新增7条注册管理器/实例入口映射，累计331/963，632条仍待核对。
所有之前分组的源码和指南哈希均验证未变。工厂类型别名已用于指南说明，但不虚增
成员数；内置插件具体行为、其他构造配置、字幕等并未据此标为完成。

Node 24.21.0 / Yarn 1.22.22：27项相关测试通过，0失败/跳过，1325.7797ms。
VitePress 11.30秒，Yarn整体13.34秒；LLM生成/只读检查、inventory与demo清点通过。
两份 TS strict/NodeNext 示例通过。Chromium 153.0.8010.12、Firefox 155.0 和
WebKit（准确版本见报告）分别通过23项注册/生命周期断言；六组页面及18次代码
转发一致，无页面错误。真实 video ready 在异步插件未完成时到达；这不代表播放、
解码、Safari真机或完整插件组合验收。中文页面截图已目视检查。

报告及输入指纹见 [证据](../baselines/site04-plugin-registry.json)。21库 build
证据仍有效；其他发布门槛保持原状，没有重绑历史媒体报告，也没有发布授权。
docs/document 与 LLM 文件由项目脚本生成；没有手改产物。回退本提交可恢复指南和
对应生成文件，核心运行时不受影响。继续字幕及剩余核心文档核对。
